---
title: 参与开发
status: active
owner: platform
last_verified: 2026-08-26
---

# 参与开发

内核是给多个项目当地基用的，所以判断一个改动该不该进来，标准和普通业务代码不一样。先读这一节再动手。

## 什么该进内核

进内核的前提是：**换一个项目，这段代码一行不改还是对的**。

不满足就留在项目里。常见的误判：

- 「加密工具」其实是某个支付网关要求的报文形态（固定 IV、特定签名算法）。这种东西发出去，会有人当通用工具用。
- 「组织架构过滤」其实是某个学校的院系模型。
- 「登录方式」其实是某个渠道的第三方协议。

判据展开见 [当前架构决策](./doc/00-architecture/60-decisions.md)。

## 一个改动要一起改哪些东西

1. **设计先行**。动权限、表、状态、对外接口时，先改 [`./doc/`](./doc/README.md) 里对应的设计文档，和代码同一批提交。说不准的标 `unverified`。
2. **默认值要能不配就跑**。新增配置项一律给安全的默认值，或者在缺配置时不注册 bean。不要让缺一行 yml 就启动失败，也不要给危险的默认值（口令、Redis 键前缀这类，宁可不给默认值直接拒绝启动）。
3. **不要让能力静默退化**。分布式锁在没有 Redis 时退单机是有意的，但这类退化必须在文档和日志里写明白——单实例开发期一切正常、上多实例才发现互斥失效，是最难查的一类问题。
4. **不新增对具体厂商的编译期依赖**。存储、消息这类要走接口 + `compileOnly`，实现类单独放，接口签名里不出现厂商类型。

## Controller 明文实现

内核和基于内核的项目都不要做 CRUD Controller 基类。`extends` 会把 URI 和权限码藏进父类，调用方看不到真实挂了哪些口。

`platform-admin` 参考 CRUD、以及业务仓自己的 Controller，每个对外方法在本类写清 HTTP 映射和 `@Permission`（或 `@PublicAccess` / `@Authenticated`）。只用到的接口才写。分页解码可以抽辅助方法，但那不是 Controller、不注册 URI。

禁止再引入 `BaseController`、按 HTTP 方法推导权限码的类注解，或任何「继承即挂口」的等价机制。全文见 [Controller 明文实现规范](./doc/20-standards/controller.md)。

## 提交与验证

提交说明格式 `type(scope): 中文摘要`，正文写为什么，不要堆文件列表。发版用 `chore: release vX.Y.Z`。

提交前在本仓库跑：

```bash
./gradlew testAll
```

采用方仓的测试不替代本仓库 `testAll`。

## 测试怎么写

测名字写清楚在验什么行为，不要 `testSaveOk` 这种。断言要对着「出错时会发生什么」，注释写清楚为什么这条值得测——比如「前缀写错等于换了一套键，在途任务全变孤儿」。

新增自动装配要同时验证「配了生效」和「没配不注册」两侧。只测生效那侧，等于没测条件装配。
