# 安全问题反馈

## 怎么报

**不要开公开 issue。** 发邮件到 security@miniants.cn，包含：

- 受影响的模块与版本
- 复现步骤，或最小复现工程
- 你判断的影响面（能读到什么、能改到什么、要不要先有凭据）

我们会在 3 个工作日内回执，修复后在发布说明里致谢（不想署名请说明）。

## 支持的版本

当前发布线是 `2.0.0-rc.x`。RC 期间只维护最新一个 RC；`2.0.0` 正式发布后维护最新一个次版本。

## 使用者需要自己把住的几条

内核提供的是机制，配错了机制不会救你。上线前对一遍：

| 项 | 要求 |
|---|---|
| `platform.security.enforcement` | 上线切 `enforce` 前须收敛未分类与本该拒绝。`shadow` 对**无票**仍 401；只对已登录 mismatch / 未分类只记不拦 |
| `platform.admin.seed` | 初始化完就从配置里移走。它不会重置已有账号的口令，但把初始口令留在 yml 里本身就是风险 |
| 口令存储 | 内核只认 bcrypt。不要为了兼容旧数据在 `PasswordEncoder` 上加明文或 MD5 分支 |
| 分布式原语 | 没有 Redis 时锁 / 幂等 / 限流会退成单机语义且**不报错**。多实例部署前确认 Redis 装配生效 |
| `/internal/runtime` | `platform-ops` 的排空接口只对 loopback 开放。不要通过反向代理把它暴露出去 |
| OAuth 客户端口令 | `sys_oauth_client.client_secret` 存的是哈希，明文只在创建 / 重置时返回一次。不要为了方便加一个「查看明文」接口 |
| JWT 与超大 Long | 业务主键可能超出 JavaScript 安全整数。客户端把它当不透明字符串，不要 `Number(id)`，也不要用 truthy 判断「有没有 ID」（`id=0` 是合法的系统管理员） |

## 依赖漏洞

依赖版本统一在 `platform-bom` 里管。报依赖漏洞时请附上 `./gradlew dependencies` 里能看到的实际版本，我们据此判断是否受影响。
